<reactiveUi:ReactiveUserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="NexusMods.App.UI.Pages.ViewModInfo.ViewModInfoView"
    x:TypeArguments="viewModFiles:IViewModFilesViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModFiles="clr-namespace:NexusMods.App.UI.Controls.ModInfo.ViewModFiles"
    xmlns:viewModInfo="clr-namespace:NexusMods.App.UI.Pages.ViewModInfo">
    <Design.DataContext>
        <viewModInfo:ViewModInfoDesignViewModel />
    </Design.DataContext>
    <ScrollViewer>
        <!-- There will be other things around this, but for now we don't have designs for switching pages etc. -->
        <reactiveUi:ViewModelViewHost x:Name="PageViewHost" />
    </ScrollViewer>
</reactiveUi:ReactiveUserControl>

