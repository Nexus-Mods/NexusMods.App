This folder contains code that do the necessary translations to migrate our
use of the V1 API to the V2 API in the legacy scenarios where results from
the website are represented in V1-only constructs.

For example, NXM links, which provide GameDomain in URL, not GameID.
